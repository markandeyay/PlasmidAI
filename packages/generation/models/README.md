# Local Model Cache

This directory is the local cache target for optional CPU generation spikes.
Model weights are intentionally ignored by git.

The current optional spike uses `HuggingFaceBio/Carbon-500M` via
`packages.generation.generator.CarbonGenerator`.
